export const metadata = {
  title: "Proses Engagement SCAN–SHIFT–SCALE–SCRIBE",
  description:
    "Tahapan kolaborasi PASSOLVING: dari diagnosis (SCAN) hingga dampak terukur (SCRIBE).",
};

export default function LayananLayout({ children }) {
  return children;
}
